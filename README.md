# ⚙️ Frota Simples - Backend (API)

O repositório do Backend (API) da aplicação **Frota Simples**.

## 🔑 Funcionalidades Principais
* Gerenciamento de **Entidades** (Carros, Funcionários, Empréstimos).
* Implementação das **Regras de Negócio** (ex: um carro só pode ser emprestado se estiver disponível).
* API RESTful para comunicação com o Frontend (Angular).
* Persistência e integridade dos dados.

## 🛠️ Tecnologias Utilizadas
* **Python:** Linguagem principal (Recomendável: Flask/Django/FastAPI).
* **PostgreSQL:** Banco de dados relacional para persistência de dados.
* **Virtualenv/Pipenv:** Para gerenciamento de dependências.

## ⚙️ Configuração e Execução

### Pré-requisitos
* Python 3.x
* Banco de dados PostgreSQL (Instalado e rodando)
